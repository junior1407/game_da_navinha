using UnityEngine;
using System.Collections;

public class PlayerStats : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//SpawnWaves ();
	
	}
	
	// Update is called once per frame
	void Update () {
		//Vector3 posicao_spawn = new Vector3(Random.Range(
	
	}
}
